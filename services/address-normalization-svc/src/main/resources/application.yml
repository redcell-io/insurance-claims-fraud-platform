spring:
  application:
    name: address-normalization-svc

# Custom property (not Spring's own grpc.* namespace) — read by
# GrpcServerLifecycle. Default 9092 matches ADDRESS_NORMALIZATION_ADDR's
# default in orchestration-service.
app:
  grpc:
    port: 9092
