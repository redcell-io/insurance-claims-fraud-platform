spring:
  application:
    name: claimant-id-hashing-svc

# Custom property (not Spring's own grpc.* namespace) — read by
# GrpcServerLifecycle. Default 9095 matches CLAIMANT_ID_HASHING_ADDR's
# default in orchestration-service.
app:
  grpc:
    port: 9095
